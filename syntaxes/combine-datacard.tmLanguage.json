{
    "scopeName": "source.combine-datacard",
    "patterns": [
        {
            "name": "keyword.combine-datacard",
            "match": "\\b(observation|bin|process|rate|shape|kmax|imax|jmax)\\b"
        },
        {
            "name": "comment.combine-datacard",
            "match": "#.*"
        },
        {
            "name": "constant.numeric.combine-datacard",
            "match": "-?\\b\\d+(?:\\.\\d+)?(?:[eE][-+]?\\d+)?\\b"
        }
    ]
}
